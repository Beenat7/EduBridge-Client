export interface Subject {
  id: string;
  schoolId: string;
  name: string;
  code: string;
  description: string;
  status: string;
}

export interface TeacherSubject {
  id: string;
  name: string;
  code: string;
  status: string;
}
